import 'package:flutter/material.dart';

class CustomPlan extends StatelessWidget {
  CustomPlan({plan, super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Text('custom plan');
  }
}